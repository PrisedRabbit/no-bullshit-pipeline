# Brownfield Detection

## Check for Prior Work

1. Read `_bmad/bmm/config.yaml` to resolve artifact paths
2. Check existence of:
   - `{implementation_artifacts}/sprint-status.yaml`
   - `{planning_artifacts}/prd*.md`
   - `{planning_artifacts}/arch*.md`
   - `{project_knowledge}/`

## Decision

- **GREENFIELD:** None exist → full enhancement + blueprint selection
- **BROWNFIELD:** Any exist → read PRD/Arch → summarize stack + context
