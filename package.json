{
  "name": "nbp",
  "private": true,
  "version": "0.4.0",
  "type": "module",
  "scripts": {
    "check": "cargo check --manifest-path src-tauri/Cargo.toml",
    "check:js": "node --check src/main.js && node --check src/pipeline-builder.js && node --check src/integrations-settings.js && node --check src/module-loader.js && node --check src/viewManager.js && node --check src/ui-health-check.js",
    "lint": "eslint \"src/**/*.js\" --max-warnings=0",
    "check:all": "bun run lint && bun run check:js && bun run check",
    "doctor": "node scripts/doctor.mjs",
    "doctor:rust": "node scripts/doctor.mjs --rust-only",
    "dev": "bun run doctor:rust && tauri dev --config src-tauri/tauri.dev.conf.json",
    "build": "tauri build",
    "tauri": "tauri",
    "build:release": "./build.sh",
    "update-packages": "rustup update && cargo update && bun update",
    "upgrade-packages": "cd src-tauri && cargo upgrade && cd .. && bun update"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@tauri-apps/cli": "^2.10.1",
    "eslint": "^10.1.0",
    "globals": "^17.4.0"
  },
  "dependencies": {
    "marked": "^17.0.5",
    "ui-ux-pro-mcp": "^1.5.0"
  }
}
