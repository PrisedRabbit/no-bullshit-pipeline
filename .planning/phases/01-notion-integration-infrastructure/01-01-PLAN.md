---
phase: 01-notion-integration-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/Cargo.toml
  - src-tauri/src/integrations.rs
  - src-tauri/src/integrations/mod.rs
  - src-tauri/src/integrations/slack.rs
  - src-tauri/src/integrations/notion.rs
  - src-tauri/src/config.rs
  - src-tauri/src/lib.rs
  - .gitignore
autonomous: true
requirements:
  - NOTN-02
  - NOTN-06
  - INTG-05

must_haves:
  truths:
    - "Notion API key can be stored and retrieved via dev-mode bypass in debug builds"
    - "Notion API key is stored in macOS Keychain in release builds"
    - "Integration profile struct can be serialized to and deserialized from JSON files in ~/.nbp/config/integrations/"
    - "Existing Slack integration commands still compile and function after module restructuring"
  artifacts:
    - path: "src-tauri/src/integrations/mod.rs"
      provides: "Shared KEYCHAIN_SERVICE constant, dev-mode credential helpers, IntegrationsConfig re-export"
      contains: "KEYCHAIN_SERVICE"
    - path: "src-tauri/src/integrations/slack.rs"
      provides: "All existing Slack integration code moved verbatim"
      contains: "add_slack_integration"
    - path: "src-tauri/src/integrations/notion.rs"
      provides: "NotionIntegrationProfile struct, supporting types, profile I/O functions"
      contains: "NotionIntegrationProfile"
    - path: "src-tauri/src/config.rs"
      provides: "get_integrations_dir() helper"
      contains: "get_integrations_dir"
    - path: "src-tauri/Cargo.toml"
      provides: "notion-client dependency"
      contains: "notion-client"
  key_links:
    - from: "src-tauri/src/integrations/notion.rs"
      to: "src-tauri/src/integrations/mod.rs"
      via: "imports shared token helpers and KEYCHAIN_SERVICE"
      pattern: "use super::"
    - from: "src-tauri/src/integrations/slack.rs"
      to: "src-tauri/src/integrations/mod.rs"
      via: "imports shared token helpers and KEYCHAIN_SERVICE"
      pattern: "use super::"
    - from: "src-tauri/src/lib.rs"
      to: "src-tauri/src/integrations"
      via: "mod integrations declaration"
      pattern: "mod integrations"
    - from: "src-tauri/src/config.rs"
      to: "src-tauri/src/integrations/mod.rs"
      via: "IntegrationsConfig import for AppSettings"
      pattern: "IntegrationsConfig"
---

<objective>
Restructure the integrations module from a flat file to a module directory, add the notion-client dependency, implement dev-mode Keychain bypass, define Notion profile types, and add config helpers for integration file storage.

Purpose: This is the foundation that all Notion commands depend on. The module restructure is required because Notion code cannot coexist in a single file with Slack. The dev-mode bypass must exist before any new credential storage code to prevent macOS Keychain permission dialogs during development. The profile types and config helpers establish the storage contract for Phase 1 commands.

Output: Compiling codebase with module directory structure, dev-mode bypass, Notion types, and profile I/O ready for Plan 02 to implement API commands.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-notion-integration-infrastructure/01-RESEARCH.md
@src-tauri/src/integrations.rs
@src-tauri/src/config.rs
@src-tauri/src/lib.rs
@src-tauri/Cargo.toml
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add notion-client dependency, restructure integrations module, implement dev-mode bypass</name>
  <files>
    src-tauri/Cargo.toml
    src-tauri/src/integrations.rs (DELETE after creating module dir)
    src-tauri/src/integrations/mod.rs
    src-tauri/src/integrations/slack.rs
    src-tauri/src/lib.rs
    .gitignore
  </files>
  <action>
    1. Add `notion-client = "1.0.11"` to `[dependencies]` in `src-tauri/Cargo.toml`.

    2. Add `.dev-credentials.json` to `.gitignore` BEFORE writing any bypass code.

    3. Create `src-tauri/src/integrations/` directory. Create `src-tauri/src/integrations/mod.rs` with:
       - Move `KEYCHAIN_SERVICE` constant here (from current `integrations.rs`)
       - Move `IntegrationsConfig` struct here (with `pub mod slack;` and `pub mod notion;`)
       - Re-export Slack types: `pub use slack::*;`
       - Implement shared credential helpers with dev-mode bypass:
         - `pub fn save_token(account_key: &str, token: &str) -> Result<(), String>` — uses `#[cfg(debug_assertions)]` to write to `.dev-credentials.json` in debug, Keychain in release
         - `pub fn get_token(account_key: &str) -> Result<String, String>` — same pattern
         - `pub fn delete_token(account_key: &str) -> Result<(), String>` — same pattern
       - The dev-mode functions read/write `.dev-credentials.json` at the project root (where `cargo tauri dev` runs). Use `serde_json::Value` as a simple key-value store keyed by `account_key`.
       - The release-mode functions use `security_framework::passwords::{set_generic_password, get_generic_password, delete_generic_password}` with `KEYCHAIN_SERVICE`.

    4. Create `src-tauri/src/integrations/slack.rs`:
       - Move ALL Slack-specific code from the old `integrations.rs`: `SlackIntegration`, `SlackAuthTestResponse`, `SlackChannel`, `SlackConversationsResponse`, `SlackChannelRaw`, and all 5 Tauri commands (`list_slack_integrations`, `add_slack_integration`, `remove_slack_integration`, `test_slack_integration`, `list_slack_channels`).
       - Update Slack's internal `save_slack_token`, `get_slack_token`, `delete_slack_token` to call the new shared `super::save_token("slack:{id}", token)`, `super::get_token("slack:{id}")`, `super::delete_token("slack:{id}")` helpers instead of directly using `security_framework`. Keep `get_slack_token` public since `connectors/slack.rs` imports it.
       - Ensure all `use` statements reference `super::KEYCHAIN_SERVICE` or the new shared helpers as needed.

    5. Delete `src-tauri/src/integrations.rs` (the flat file). The `mod integrations;` in `lib.rs` will now resolve to `integrations/mod.rs` automatically — no change needed in lib.rs for this.

    6. Verify: `config.rs` imports `crate::integrations::IntegrationsConfig` — this must still work after the restructure. The `IntegrationsConfig` struct is in `mod.rs` and re-exported, so the import path remains the same.

    7. Run `cargo check` to verify compilation. Fix any import path issues. The critical paths to verify:
       - `crate::integrations::IntegrationsConfig` (used in config.rs)
       - `crate::integrations::get_slack_token` (used in connectors/slack.rs)
       - All 5 Slack Tauri commands in `lib.rs` invoke_handler
  </action>
  <verify>
    Run `cargo check` in `src-tauri/`. All existing code compiles. No warnings about unused imports from the module restructure. Verify `.dev-credentials.json` is in `.gitignore`.
  </verify>
  <done>
    The integrations module is a directory with mod.rs + slack.rs. Dev-mode credential bypass compiles. notion-client is in Cargo.toml. All existing Slack commands compile and the invoke_handler paths are unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Define Notion profile types, profile I/O, and config helper</name>
  <files>
    src-tauri/src/integrations/notion.rs
    src-tauri/src/config.rs
  </files>
  <action>
    1. Add `get_integrations_dir()` to `src-tauri/src/config.rs`:
       ```rust
       pub fn get_integrations_dir() -> PathBuf {
           get_config_dir().join("config").join("integrations")
       }
       ```
       Wait — `get_config_dir()` already returns `~/.nbp`. The integrations dir should be `~/.nbp/config/integrations/` per the decision. But `get_config_dir()` returns `~/.nbp`, and `get_settings_path()` returns `~/.nbp/settings.json`. So the config directory IS `~/.nbp`. The integrations path should be `get_config_dir().join("integrations")` to produce `~/.nbp/integrations/`. BUT the decision says `~/.nbp/config/integrations/`. Check: the PROJECT.md says "Config in `~/.nbp/config/`". Look at `get_settings_path()` which returns `get_config_dir().join("settings.json")` and `get_config_dir()` returns `~/.nbp`. So settings.json is at `~/.nbp/settings.json`. The decision to use `~/.nbp/config/integrations/` is based on the research saying "config in `~/.nbp/config/`" but the actual code uses `~/.nbp/` as the config root. Follow the ACTUAL codebase convention: `get_config_dir().join("integrations")` → `~/.nbp/integrations/`. This matches how templates are at `get_config_dir().join("templates")` → `~/.nbp/templates/`.

       Correct implementation:
       ```rust
       pub fn get_integrations_dir() -> PathBuf {
           get_config_dir().join("integrations")
       }
       ```

    2. Create `src-tauri/src/integrations/notion.rs` with:

       **Types (all derive Serialize, Deserialize, Clone, Debug):**
       - `NotionIntegrationProfile { id: String, name: String, database_id: String, database_name: String, properties: Vec<NotionPropertyDef>, people_mappings: Vec<PeopleMapping>, workspace_users: Vec<WorkspaceUser>, synced_at: String }`
       - `NotionPropertyDef { name: String, property_type: String, select_options: Vec<String> }` — property_type is one of: "title", "rich_text", "select", "multi_select", "people", "date", "number", "checkbox", "url", "email", "phone_number", "formula", "relation", "rollup", "created_time", "last_edited_time", "status"
       - `PeopleMapping { alias: String, notion_user_id: String, display_name: String }`
       - `WorkspaceUser { id: String, name: Option<String> }`

       **Profile I/O functions:**
       - `pub fn save_notion_profile(profile: &NotionIntegrationProfile) -> Result<(), String>` — creates dir with `fs::create_dir_all`, writes to `config::get_integrations_dir().join(format!("notion-{}.json", profile.id))`, uses `serde_json::to_string_pretty`, sets file permissions to 0o600
       - `pub fn load_notion_profile(integration_id: &str) -> Result<NotionIntegrationProfile, String>` — reads from same path, deserializes, error message: "Notion integration '{}' profile not found. Sync schema in Settings > Integrations."
       - `pub fn delete_notion_profile(integration_id: &str) -> Result<(), String>` — removes the file
       - `pub fn list_notion_profiles() -> Result<Vec<NotionIntegrationProfile>, String>` — reads all `notion-*.json` files from integrations dir, deserializes each

       **Token helper wrappers (call shared helpers in mod.rs):**
       - `pub fn save_notion_token(integration_id: &str, token: &str) -> Result<(), String>` — calls `super::save_token(&format!("notion:{}", integration_id), token)`
       - `pub fn get_notion_token(integration_id: &str) -> Result<String, String>` — calls `super::get_token(...)`
       - `pub fn delete_notion_token(integration_id: &str) -> Result<(), String>` — calls `super::delete_token(...)`

       Do NOT add any Tauri commands yet — those come in Plan 02.

    3. Run `cargo check` to verify all types compile and profile I/O functions are syntactically correct.
  </action>
  <verify>
    Run `cargo check` in `src-tauri/`. The new types in `notion.rs` compile. The `get_integrations_dir()` function exists in config.rs. No dead code warnings are acceptable for now (types will be used in Plan 02).
  </verify>
  <done>
    `NotionIntegrationProfile` and supporting types are defined. Profile save/load/delete/list functions work against `~/.nbp/integrations/notion-{id}.json`. Token helper wrappers delegate to shared dev-mode-aware credential functions. `cargo check` passes.
  </done>
</task>

</tasks>

<verification>
1. `cargo check` passes in `src-tauri/` — the full project compiles
2. Module structure exists: `src-tauri/src/integrations/mod.rs`, `slack.rs`, `notion.rs`
3. Old `src-tauri/src/integrations.rs` flat file is deleted
4. `.dev-credentials.json` is in `.gitignore`
5. `notion-client = "1.0.11"` is in Cargo.toml
6. `get_integrations_dir()` exists in config.rs
7. `NotionIntegrationProfile` struct is defined with all required fields
</verification>

<success_criteria>
- `cargo check` passes with zero errors
- Existing Slack integration commands are unaffected (paths unchanged in invoke_handler)
- Dev-mode bypass compiles for both debug and release configurations
- Profile types are ready for Plan 02 to implement Tauri commands
</success_criteria>

<output>
After completion, create `.planning/phases/01-notion-integration-infrastructure/01-01-SUMMARY.md`
</output>
