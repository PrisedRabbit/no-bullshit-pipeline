---
phase: 11-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main.js
  - src/styles.css
autonomous: true
requirements:
  - UX-01

must_haves:
  truths:
    - "When user has 0 pipelines, the chip bar is completely hidden — no empty space or container visible"
    - "When user has 1–5 pipelines, all show as chips with no overflow button"
    - "When user has >5 pipelines, exactly 5 chips show with a +N overflow button that reveals the rest in a dropdown"
    - "Overflow menu items are clickable and behave identically to visible chips (start recording or mid-recording assign)"
  artifacts:
    - path: "src/main.js"
      provides: "renderPipelineChips() with edge-case handling for 0, 1–N, >N pipelines"
      contains: "chipBar.style.display"
    - path: "src/styles.css"
      provides: "Pipeline chip bar styles including hidden state"
  key_links:
    - from: "src/main.js renderPipelineChips()"
      to: "allPipelineDefs"
      via: "reads pipeline definitions to determine chip count and overflow"
      pattern: "allPipelineDefs"
---

<objective>
Harden the pipeline chip bar overflow menu for edge cases: 0 pipelines (hide bar entirely), 1–5 pipelines (no overflow button), >5 pipelines (overflow with proper keyboard accessibility and scroll for very large collections).

Purpose: Users with large or empty pipeline collections get a clean, correct chip bar without visual glitches or empty UI elements.
Output: Updated renderPipelineChips() with edge-case handling and improved overflow popover.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-pre-assignment-ux-and-execution/06-01-SUMMARY.md

Key context:
- renderPipelineChips() in src/main.js (line ~1482) already implements a 5-chip cap with +N overflow button and a popover dropdown
- showOverflowPopover() (line ~1525) creates the popover with click-to-dismiss
- CSS classes: .pipeline-chip-bar, .pipeline-chip, .chip-overflow-btn, .chip-overflow-popover
- allPipelineDefs is the global array of pipeline definitions loaded from backend
- The chip bar container is `<div id="pipeline-chip-bar" class="pipeline-chip-bar">` in src/index.html inside #capture-section
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add edge-case handling to renderPipelineChips() and improve overflow popover</name>
  <files>src/main.js, src/styles.css</files>
  <action>
In src/main.js, update renderPipelineChips() (line ~1482):

1. **0 pipelines**: When allPipelineDefs is undefined or empty, hide the chip bar entirely with `chipBar.style.display = 'none'` and return early. Currently it only sets innerHTML to empty string, leaving an empty container visible.

2. **Show bar when pipelines exist**: At the start of the rendering path (after the empty check), set `chipBar.style.display = ''` (or `'flex'`) to ensure the bar is visible when there are pipelines. This handles the case where the bar was previously hidden and pipelines were added later.

3. **Overflow popover max-height**: In showOverflowPopover(), when the overflow list is very large (e.g., 20+ pipelines), the popover could extend off screen. Add `max-height: 240px` and `overflow-y: auto` to the popover element's style so it scrolls when there are many items. Also add these to the .chip-overflow-popover CSS class in src/styles.css.

4. **Keyboard accessibility for overflow button**: Add `aria-label="Show more pipelines"` and `aria-haspopup="true"` attributes to the overflow button in the rendered HTML. Also add `role="menu"` to the popover container and `role="menuitem"` to each chip inside the popover in showOverflowPopover().

Do NOT change the MAX_CHIPS constant (5) or any click/assignment logic — only edge-case visibility and accessibility.
  </action>
  <verify>
Structurally verify:
- renderPipelineChips() sets chipBar.style.display = 'none' when 0 pipelines
- renderPipelineChips() sets chipBar.style.display = '' (or 'flex') when pipelines exist
- showOverflowPopover() sets max-height and overflow-y on the popover element
- Overflow button HTML includes aria-label and aria-haspopup attributes
- .chip-overflow-popover CSS has max-height: 240px and overflow-y: auto
  </verify>
  <done>
- 0 pipelines: chip bar is display:none (hidden, no empty space)
- 1-5 pipelines: chip bar visible, all chips shown, no overflow button
- 6+ pipelines: chip bar visible, 5 chips + overflow button, popover scrolls for large collections
- Overflow button has aria attributes for accessibility
- Popover has role="menu" and items have role="menuitem"
  </done>
</task>

</tasks>

<verification>
1. Read src/main.js renderPipelineChips() and confirm edge-case handling for 0, 1-5, and 6+ pipelines
2. Read src/styles.css and confirm .chip-overflow-popover has max-height and overflow-y
3. Confirm no changes to MAX_CHIPS, handleChipClick, startRecordingWithPipeline, or any Rust backend code
</verification>

<success_criteria>
- renderPipelineChips() hides chip bar when 0 pipelines and shows it when pipelines exist
- Overflow popover scrolls for large collections (max-height + overflow-y)
- Overflow button and popover have ARIA attributes
- All existing chip click behavior preserved
</success_criteria>

<output>
After completion, create `.planning/phases/11-ux-polish/11-01-SUMMARY.md`
</output>
